Main Menu:
    - Username
    - Multiplayer (can't press if no username)
        - Create lobby (or do you want to make this its own option?)
            - Put in name
        - Join lobby (lobbies are in a list. double click or select and press "join lobby" to join)
            - Username can't already be in lobby
    - Options

    - Exit

Options:
    -

Find lobby menu:

    - Lobbies list
        (shows username, lobby name)
    - Join button (greyed out when lobby not selected)
    - Refresh button (or do you just want to live refresh?)

Create lobby
    - When lobby is created, prompt username